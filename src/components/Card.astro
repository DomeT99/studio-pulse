---
export interface Props {
    title: string;
    icon?: string;
}

const { title, icon } = Astro.props;
---

<div class="box">
    {icon && <span class="icon-lg mb-4" aria-hidden="true">{icon}</span>}
    <h3 class="title is-4">{title}</h3>
    <p><slot /></p>
</div>